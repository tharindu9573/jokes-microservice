ARG RMQ_SUBMITTER_URL
ARG RMQ_MODERATOR_URL

FROM node:slim

ENV RMQ_SUBMITTER_URL=$RMQ_SUBMITTER_URL
ENV RMQ_MODERATOR_URL=$RMQ_MODERATOR_URL
ENV NODE_ENV development

WORKDIR /express-docker

COPY . .

RUN npm install

CMD ["npm", "start"]

EXPOSE 3100